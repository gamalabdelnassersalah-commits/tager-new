{
  "name": "tager-v23-enterprise-production-platform",
  "version": "23.0.0",
  "private": true,
  "scripts": {
    "build": "node build-static.mjs",
    "check": "node --check app.js && node --check supabase-client.js"
  },
  "engines": {
    "node": ">=18"
  }
}