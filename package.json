{
  "name": "tager-v24-enterprise-full-audit-platform",
  "version": "24.0.0",
  "private": true,
  "scripts": {
    "build": "node build-static.mjs",
    "check": "node --check app.js && node --check supabase-client.js"
  },
  "engines": {
    "node": ">=18"
  }
}