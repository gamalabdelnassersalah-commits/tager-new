{
  "name": "tager-platform",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "build": "node build-static.mjs",
    "check": "node --check app.js"
  },
  "dependencies": {
    "serve": "latest"
  }
}
